

{/* 


     



{this.state.produits.map( product => {
        return(
          <li key={product.src}>
            <div className="product">
              <h3>{product.auteur}</h3>
              <p>{product.montant}</p>
              <button type="button" onClick={this.Addproduct.bind(this, product.src, product.auteur, product.montant)}>ADD TO CART</button>
            </div>
          </li>
        )
      })}

*/}